<h1>Edit Group</h1>
<%= form_for @group, :url => {:action => "update"} do |f| %>
	<%= render :partial => 'form', :locals => {:f => f } %>
	<div class="actions">
		<%= f.submit %>
	</div>
<%end%>
<%= link_to 'Back', admin_groups_path %>
