<h1>New Job Group</h1>
<%= form_for @job_group, :url => { :action => "create_job_group", :id => @group.id } do |f| %>
    <%= render :partial => 'job_group_form', :locals => { :f => f } %>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
<%= link_to 'Back', admin_groups_path(:id => @group.id) %>
