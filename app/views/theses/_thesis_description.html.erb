<div class="join-request">
  <div class="w100">
    <div class="groups-heading-hold">

      <h1><%= @thesis.name %></h1>

      <p> <%= @thesis.created_at.strftime('%m/%d/%Y') %>  -

      </p>
    </div>
           <%unless @thesis.photo.blank? %>
    <div class="join-group-hold"><%= link_to t('Thesis.download_thesis'), download_thesis_theses_path(:id => @thesis)%></div>
            <%end%>
  </div>

  <div class="group-details-hold">
    <div class="grpup-img-details-hold" style="float: left;"><%= @thesis.description %>
    </div>
  </div>
</div>
